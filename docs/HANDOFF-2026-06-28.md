# HANDOFF — 2026-06-28
> Documento de continuidade de sessão. Retomar exatamente deste ponto.

---

## Resumo Executivo

- Sprint 1 concluído — 10 Quick Wins aplicados na landing page.
- Sprint 2 concluído — infraestrutura do funil de aquisição implementada.
- Landing em produção em `https://checklist.recrutapadrao.com.br`.
- Build validada — zero erros, zero warnings.
- PM2 operacional — processo `recrutapadrao-checklist` online (id=5).
- Persistência de leads no Supabase funcionando — insert e rollback validados.
- API `POST /api/leads` validada em produção.
- Tabela `leads_checklist` criada, testada e com RLS + grants corretos.

---

## Estado do Git

| Item | Valor |
|---|---|
| Branch | `master` |
| Último commit | `1c10a20 fix(security): remover credenciais hardcoded da Evolution` |
| Remote | `git@github.com:gadelhajulio-crypto/recrutapadrao-checklist.git` |
| Árvore | Limpa — apenas `docs/auditoria-checklist/` untracked (intencional) |

### Commits desta sessão (mais recente primeiro)

```
1c10a20  fix(security): remover credenciais hardcoded da Evolution
398b28e  docs(funil): arquitetura, guia de ativação e plano de testes E2E
36464a5  chore(funil): adicionar script de criação de instância Evolution API
a9a994b  chore(funil): instalar @supabase/supabase-js e atualizar .env.example
3f5e598  feat(funil): conectar formulário ao endpoint /api/leads
0cc03f7  feat(funil): adicionar rotas API — POST /api/leads e webhook WhatsApp
79cf40c  feat(funil): adicionar cliente Evolution API (sendText + sendDocument)
e0f11d1  feat(funil): ativar cliente Supabase servidor com service_role
37e07a9  copy(checklist): adicionar bloco de autoridade institucional
0869378  copy(checklist): adicionar microcopy de privacidade
b2c3342  copy(checklist): melhorar tela de sucesso
48b0599  copy(checklist): melhorar headline do formulário
```

---

## Estado da Infraestrutura

### PM2

| id | Processo | Status | Restarts |
|---|---|---|---|
| 0 | pm2-logrotate | online | 0 |
| 1 | vitrinni-os | online | 0 |
| 2 | recruta-padrao-os | online | 0 |
| 3 | qd-stale-checker | stopped | — |
| 4 | mergulho-atipico | online | 0 |
| 5 | **recrutapadrao-checklist** | **online** | 3 |

Porta: `3011` → Nginx → `https://checklist.recrutapadrao.com.br`

### Supabase

- Projeto: `fjwvtzvfbhubxicsmbdz`
- Tabela: `leads_checklist` — criada, RLS habilitado, grants corretos
- Credenciais: `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` em `/var/www/recrutapadrao-checklist/.env.local`
- Status: **ATIVO** — persistência funcionando em produção

### Evolution API

- Container: `evolution_api` — Up 2 days em `127.0.0.1:8080`
- Stack: docker-compose em `/opt/evolution-api/`
- Config: `/opt/evolution-api/.env` → variável `AUTHENTICATION_API_KEY`
- Instâncias WhatsApp: **0 configuradas**
- Status: **rodando, sem instância conectada**
- **ATENÇÃO: chave `AUTHENTICATION_API_KEY` comprometida — rotação pendente (ver Pendências)**

### Docker

| Container | Imagem | Status | Porta |
|---|---|---|---|
| `evolution_api` | evoapicloud/evolution-api:latest | Up 2 days | 127.0.0.1:8080 |
| `evolution_postgres` | postgres:15 | Up 2 days | interno |
| `evolution_redis` | redis:7 | Up 2 days | interno |
| `vitrinni-studio_db_1` | postgres:16-alpine | Up 2 days (healthy) | 127.0.0.1:5432 |

### Nginx

- Configuração: OK (`nginx -t` passou)
- Sites ativos em `/etc/nginx/sites-enabled/`:
  - `recrutapadrao-checklist` → proxy para `127.0.0.1:3011`
  - `os.recrutapadrao.com.br`, `recrutapadrao.com.br`
  - `vitrinni`, `vitrinni-app`, `os.vitrinni.app`
  - `mergulho-atipico`, `harmonize`, `evo-vitrinni`

### n8n

- Status: **não instalado / não rodando**
- Porta 5678: sem resposta
- Não está no Docker

---

## O que está funcionando

| Componente | Status |
|---|---|
| Landing page `https://checklist.recrutapadrao.com.br` | ✅ Online |
| Redirect `/` → `/checklist` (permanente 301) | ✅ |
| Formulário de captura de leads | ✅ |
| `POST /api/leads` — validação + persistência | ✅ |
| Supabase — tabela `leads_checklist` | ✅ Ativo |
| `POST /api/webhooks/whatsapp` — receiver de eventos | ✅ Deployado |
| `lib/evolution.ts` — cliente Evolution API | ✅ Deployado (aguarda instância) |
| `scripts/setup-evolution.sh` — criação de instância | ✅ Pronto para uso |
| `.env.example` — documentação de variáveis | ✅ |
| `docs/funil/` — arquitetura, ativação, testes | ✅ |
| PM2 state salvo (`pm2 save`) | ✅ |
| Build Next.js 15 sem erros | ✅ |
| SEO title/meta atualizado | ✅ Sprint 1 |
| Redirect permanente | ✅ Sprint 1 |
| Hero visual sem placeholder de texto | ✅ Sprint 1 |
| Headline do formulário | ✅ Sprint 1 |
| Tela de sucesso com copy definitiva | ✅ Sprint 1 |
| Microcopy de privacidade (LGPD) | ✅ Sprint 1 |
| Bloco de autoridade institucional | ✅ Sprint 1 |

---

## O que está pendente

### Alta prioridade

#### 1. Rotacionar `AUTHENTICATION_API_KEY` da Evolution API

A chave foi exposta publicamente no commit `36464a5` (GitHub). **Considerar comprometida.**

Arquivos a atualizar (nomes das variáveis apenas — nunca registrar valores):

| Arquivo | Variável |
|---|---|
| `/opt/evolution-api/.env` | `AUTHENTICATION_API_KEY` |
| `/var/www/recrutapadrao-checklist/.env.local` | `EVOLUTION_API_KEY` |
| `/var/www/vitrinni-os/apps/web/.env.local` | `EVOLUTION_API_KEY` |
| `/root/vitrinni-studio/apps/web/.env.local` | `EVOLUTION_API_KEY` |

Sequência segura documentada em: **Próxima Sessão → Item 1**

#### 2. Criar instância Evolution API

- Nome da instância: `recrutapadrao`
- Comando: `bash scripts/setup-evolution.sh` (após rotação da chave)
- Variável a configurar: `EVOLUTION_INSTANCE_NAME=recrutapadrao`

#### 3. Conectar WhatsApp Business

- Nome comercial: Recruta Padrão
- Categoria: Site educacional
- Número: `5584920000210`
- Variável a configurar: `NEXT_PUBLIC_WHATSAPP_NUMBER=5584920000210`

#### 4. Hospedar o PDF oficial do Checklist

- Hospedagem recomendada: Supabase Storage (bucket público `checklist`)
- Variável a configurar: `CHECKLIST_PDF_URL=<url_publica_do_pdf>`

#### 5. Configurar n8n (opcional — automações avançadas)

- Variável a configurar: `N8N_WEBHOOK_URL=<url_do_webhook_n8n>`
- n8n não está instalado no servidor — decidir: instalar local ou usar cloud

#### 6. Teste E2E completo do funil

Somente após itens 1–4:

```
Landing → Formulário → POST /api/leads
  → Supabase (insert validado)
  → Evolution API (mensagem de boas-vindas)
  → WhatsApp (entrega do PDF)
  → n8n (webhook — se configurado)
```

Plano completo em: `docs/funil/02-plano-de-testes.md`

---

## Nunca registrar

Os documentos desta pasta nunca devem conter:

- API Keys
- Tokens JWT
- Senhas
- Service Role Keys
- Secrets de qualquer natureza

Registrar **apenas nomes das variáveis**, nunca valores.

---

## Próxima Sessão — Checklist numerado

Execute nesta ordem exata. Não pular etapas.

**1. Rotação da Evolution API key (segurança — executar primeiro)**
   1. Gerar nova chave: `openssl rand -base64 24 | tr -d '/+=' | cut -c1-32`
   2. Editar `/opt/evolution-api/.env` → `AUTHENTICATION_API_KEY=<nova_chave>`
   3. `cd /opt/evolution-api && docker compose restart api`
   4. Testar nova chave: `curl http://127.0.0.1:8080/instance/fetchInstances -H "apikey: <nova_chave>"`
   5. Editar os três `.env.local` com `EVOLUTION_API_KEY=<nova_chave>`
   6. `pm2 restart recrutapadrao-checklist`
   7. Confirmar rejeição da chave antiga

**2. Criar instância Evolution**
   1. `bash /var/www/recrutapadrao-checklist/scripts/setup-evolution.sh`
   2. Escanear QR code com WhatsApp Business do número `5584920000210`
   3. Confirmar conexão via API

**3. Configurar variáveis de ambiente**
   1. `EVOLUTION_INSTANCE_NAME=recrutapadrao` em `.env.local`
   2. `NEXT_PUBLIC_WHATSAPP_NUMBER=5584920000210` em `.env.local`
   3. `CHECKLIST_PDF_URL=<url>` em `.env.local` (após hospedar PDF)

**4. Hospedar o PDF**
   1. Fazer upload do PDF no Supabase Storage (bucket `checklist`, público)
   2. Copiar URL pública → `CHECKLIST_PDF_URL`

**5. Build e deploy**
   1. `npm run build` — validar zero erros
   2. `pm2 restart recrutapadrao-checklist`
   3. `pm2 save`

**6. Teste E2E completo** (seguir `docs/funil/02-plano-de-testes.md`)
   1. T1 — Submissão válida + WhatsApp + PDF
   2. T2 / T3 — Validações de formulário
   3. T4 — API direta via curl
   4. T5 — Webhook Evolution
   5. T7 — UTMs propagados
   6. T8 — Mobile

**7. Verificar vitrinni-os**
   1. Confirmar que `EVOLUTION_API_KEY` atualizado está funcionando
   2. Testar fluxo afetado no vitrinni-os

**8. Encerramento da sessão**
   1. `git status` — confirmar árvore limpa
   2. Varredura de credenciais nos arquivos versionados
   3. Atualizar este HANDOFF ou criar `HANDOFF-2026-06-29.md`
